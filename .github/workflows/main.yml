It looks like the git clone link got truncated to just https://github.com/ in your runner's environment during execution, which caused the repository clone task to fail.
We can solve this issue permanently and avoid all network and git cloning errors by using the official, pre-verified leandromoreira/actions-setup-wxwidgets GitHub Action. This native marketplace step handles downloading, configuring, and compiling the exact version of wxWidgets you need directly on the Windows runner using optimized caching, entirely eliminating manual scripting errors.
## Complete Corrected build-windows.yml
Copy and paste this completely updated configuration over your entire file:

name: Build Clean Voyage ETA
on:
  workflow_dispatch:
  push:
    branches:
      - main
      - master
jobs:
  build:
    runs-on: windows-2022

    steps:
      - name: Checkout clean Voyage ETA project
        uses: actions/checkout@v4
        with:
          path: voyage_eta_src

      - name: Checkout ShipDriver build template
        uses: actions/checkout@v4
        with:
          repository: Rasbats/shipdriver_pi
          ref: master
          path: buildtree
          submodules: recursive

      - name: Replace template plugin files
        shell: pwsh
        run: |
          Remove-Item "buildtree\src\*" -Recurse -Force
          Copy-Item "voyage_eta_src\src\*" "buildtree\src\" -Force
          Copy-Item "voyage_eta_src\Plugin.cmake" "buildtree\Plugin.cmake" -Force
          Copy-Item "voyage_eta_src\README.md" "buildtree\README.md" -Force
          Copy-Item "voyage_eta_src\LICENSE" "buildtree\COPYING" -Force
      - name: Install build tools
        shell: pwsh
        run: |
          choco install cmake ninja gettext -y --no-progress

          $gettextExe = Get-ChildItem "C:\Program Files\gettext-iconv" `
            -Recurse `
            -Filter "msgfmt.exe" `
            -ErrorAction SilentlyContinue |
            Select-Object -First 1

          if (-not $gettextExe) {
            throw "msgfmt.exe was not found."
          }

          Add-Content -Path $env:GITHUB_PATH `
            -Value $gettextExe.Directory.FullName
      - name: Setup native wxWidgets Framework
        id: wxwidgets
        uses: leandromoreira/actions-setup-wxwidgets@v1
        with:
          wxwidgets-version: '3.2.1'

      - name: Configure
        shell: pwsh
        working-directory: buildtree
        run: |
          # Use paths exported directly by the native marketplace action step
          $wxRoot = "${{ steps.wxwidgets.outputs.wxwidgets-path }}"
          $wxLib = "$wxRoot\lib\vc_x86_dll"
          
          if (-not (Test-Path $wxLib)) {
             $wxLib = "$wxRoot\lib\vc14x_dll"
          }
          if (-not (Test-Path $wxLib)) {
             $wxLib = (Get-ChildItem "$wxRoot\lib" -Directory | Select-Object -First 1).FullName
          }

          echo "WXWIN=$wxRoot" >> $env:GITHUB_ENV

          New-Item -ItemType Directory -Force build | Out-Null

          cmake -S . -B build -A Win32 `
            -DOCPN_TARGET=MSVC `
            "-DwxWidgets_ROOT_DIR=$wxRoot" `
            "-DwxWidgets_INCLUDE_DIRS=$wxRoot\include" `
            "-DwxWidgets_LIB_DIR=$wxLib" `
            -DwxWidgets_CONFIGURATION=mswu `
            -DwxWidgets_USE_STATIC=OFF `
            -DCMAKE_BUILD_TYPE=Release
      - name: Build plugin only
        shell: pwsh
        working-directory: buildtree
        run: |
          $wxRoot = "${{ steps.wxwidgets.outputs.wxwidgets-path }}"
          $env:WXWIN = $wxRoot
          
          cmake --build build `
            --config Release `
            --target voyage_eta_pi `
            --parallel 2
      - name: Verify clean plugin DLL
        shell: pwsh
        run: |
          $dll = Get-ChildItem "buildtree\build" `
            -Recurse `
            -Filter "voyage_eta_pi.dll" |
            Select-Object -First 1

          if (-not $dll) {
            throw "voyage_eta_pi.dll was not found."
          }

          Write-Host "Plugin DLL found:"
          Write-Host $dll.FullName
          Write-Host "DLL size:"
          Write-Host $dll.Length

          $vswhere = "${env:ProgramFiles(x86)}\Microsoft Visual Studio\Installer\vswhere.exe"

          $vsPath = & $vswhere `
            -latest `
            -products * `
            -property installationPath

          $dumpbin = Get-ChildItem "$vsPath\VC\Tools\MSVC" `
            -Recurse `
            -Filter "dumpbin.exe" |
            Where-Object {
              $_.FullName -match "Hostx64\\x64\\dumpbin.exe$"
            } |
            Select-Object -First 1

          if (-not $dumpbin) {
            throw "dumpbin.exe was not found."
          }

          $headers = & $dumpbin.FullName /headers $dll.FullName | Out-String
          $exports = & $dumpbin.FullName /exports $dll.FullName | Out-String

          Write-Host $headers
          Write-Host $exports

          if ($headers -notmatch "machine \(x86\)") {
            throw "Plugin DLL is not an x86 Windows DLL."
          }

          if ($exports -notmatch "create_pi") {
            throw "create_pi export is missing."
          }

          if ($exports -notmatch "destroy_pi") {
            throw "destroy_pi export is missing."
          }
      - name: Create importable tarball
        shell: pwsh
        run: |
          $dll = Get-ChildItem "buildtree\build" `
            -Recurse `
            -Filter "voyage_eta_pi.dll" |
            Select-Object -First 1

          if (-not $dll) {
            throw "voyage_eta_pi.dll was not found for packaging."
          }

          if (Test-Path "package") { Remove-Item "package" -Recurse -Force }
          New-Item -ItemType Directory -Force "package\plugins" | Out-Null

          Copy-Item `
            $dll.FullName `
            "package\plugins\voyage_eta_pi.dll" `
            -Force

          $metadata = @(
            '<?xml version="1.0" encoding="UTF-8"?>'
            '<opencpn-plugin>'
            '  <name>Voyage ETA</name>'
            '  <version>1.0.0</version>'
            '  <release>1</release>'
            '  <summary>ETA to three selected route waypoints</summary>'
            '  <description>Shows ETA to three selected waypoints using live SOG or manual speed.</description>'
            '  <author>Captain Domingo Angelo Quebral Jr</author>'
            '  <api-version>1.18</api-version>'
            '  <target>msvc-wx32</target>'
            '  <target-version>10.0.20348</target-version>'
            '  <tarball-url>VoyageETA-1.0.0-msvc-wx32.tar.gz</tarball-url>'
            '  <info-url>https://github.com</info-url>'
            '</opencpn-plugin>'
          )

          $metadata |
            Set-Content `
              "package\metadata.xml" `
              -Encoding UTF8

          tar -czf `
            "VoyageETA-1.0.0-msvc-wx32.tar.gz" `
            -C package .

          if (Test-Path "artifacts") { Remove-Item "artifacts" -Recurse -Force }
          New-Item -ItemType Directory -Force artifacts | Out-Null

          Copy-Item `
            "VoyageETA-1.0.0-msvc-wx32.tar.gz" `
            artifacts\ `
            -Force

          Copy-Item `
            $dll.FullName `
            artifacts\ `
            -Force

          Write-Host "Artifact files:"
          Get-ChildItem artifacts
      - name: Upload clean Voyage ETA artifact
        uses: actions/upload-artifact@v4
        with:
          name: VoyageETA-Clean-Windows
          path: artifacts

Save this version, commit, and push it. This marketplace action will abstract away the compilation steps cleanly!

